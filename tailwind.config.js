/** @type {import('tailwindcss').Config} */

/**
 * Палитра «Изразец».
 *
 * Взята из того, что турист реально видит в поездке: бирюза майолики
 * Куня-Ургенча и Мерва, песок пустыни, обожжённый кирпич. Прежняя палитра
 * состояла из пяти почти одинаковых синих без акцента, поэтому кнопки
 * не выделялись, а конкуренты (Advantour — голубой, Stantrips — тёмно-синий)
 * выглядели похоже.
 *
 * Правило одно: бирюза — это бренд и навигация, кирпич — только деньги
 * и действия. Если красить кирпичом что-то ещё, акцент перестаёт работать.
 */

const palette = {
  // Бирюза изразца — бренд
  tile: "#0F5257", // шапка, подвал, заголовки; белый текст даёт контраст ~9:1
  tileDark: "#0A3A3E", // наведение на тёмном, тени
  tileMid: "#1B7A7E", // плашки с белым текстом; ~4.9:1, проходит AA
  tileLight: "#2E9B9B", // ссылки и наведение на светлом фоне
  tileTint: "#E3F0EF", // подложки блоков и активные состояния

  // Обожжённый кирпич — акцент. Только цены и кнопки действия
  brick: "#B4441F", // ~5.5:1 с белым текстом
  brickDark: "#8E3517", // наведение

  // Песок пустыни — тёплый нейтральный
  sand: "#EDE3D2",
  sandLight: "#F7F1E7",

  // Основа
  paper: "#FBFAF7", // фон страницы, чуть тёплее белого
  ink: "#15201F", // основной текст
  inkMuted: "#5D6B6A", // второстепенный текст, ~5.3:1 на белом
};

/**
 * Старые названия оставлены осознанно: они встречаются в 47 файлах, и разом
 * переименовывать их — лишний риск без пользы. Здесь они переуказаны на новую
 * палитру, поэтому вся существующая вёрстка подхватывает цвета автоматически.
 * В новом коде используйте семантические имена выше.
 */
const legacy = {
  mainBlue: palette.tile,
  mainSkyBlue: palette.tileMid,
  mainNormBlue: palette.brick, // кнопки брони: были бледно-голубыми и читались как неактивные
  mainBlueGray: palette.inkMuted,
  mainLight: palette.sand,
  mainForBackground: palette.tileTint,
  activeColor: palette.brick, // раньше было "BF8B30" без решётки — невалидный цвет, класс молча не работал
};

module.exports = {
  content: ["./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      colors: { ...palette, ...legacy },
      backgroundColor: { ...palette, ...legacy },
      top: {
        "90p": "90%",
      },
      screens: {
        bas: "500px",
        dort: "400px",
      },
      fontSize: {
        md: "16px",
      },
      fontFamily: {
        comforta: ["var(--font-comforta)", "sans-serif"],
        poppins: ["var(--font-poppins)", "latin"],
        quicksand: ["var(--font-quicksand)", "sans-serif"],
        montserrat: ["var(--font-montserrat)", "sans-serif"],
      },
    },
  },
  plugins: [],
};
